server:
  http_listen_port: 3200

distributor:
  receivers:
    zipkin:
      endpoint: "tempo:9411"

storage:
  trace:
    backend: local
    wal:
      path: /var/tempo/wal
    local:
      path: /var/tempo/blocks
